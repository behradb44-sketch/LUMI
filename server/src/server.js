import http from "node:http";import fs from "node:fs";import path from "node:path";import {fileURLToPath} from "node:url";import {PORT} from "./config.js";import {attach} from "./websocket/websocketServer.js";
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"../..");
const MIME={".html":"text/html; charset=utf-8",".js":"text/javascript; charset=utf-8",".css":"text/css; charset=utf-8",".json":"application/json",".png":"image/png",".jpg":"image/jpeg",".jpeg":"image/jpeg",".webp":"image/webp",".svg":"image/svg+xml",".ico":"image/x-icon"};
const server=http.createServer((req,res)=>{res.setHeader("Access-Control-Allow-Origin","*");res.setHeader("Access-Control-Allow-Headers","*");if(req.url==="/health"){res.writeHead(200,{"Content-Type":"application/json"});return res.end(JSON.stringify({status:"ok",game:"LUMI",version:"1.4.0"}))}let pathname="/";try{pathname=new URL(req.url,"http://localhost").pathname}catch{};if(pathname.startsWith("/server/")){res.writeHead(403);return res.end("Forbidden")}let file=path.resolve(ROOT,"."+pathname);if(pathname==="/"||!path.extname(file))file=path.join(ROOT,"index.html");if(!file.startsWith(ROOT)){res.writeHead(403);return res.end("Forbidden")}fs.stat(file,(err,st)=>{if(err||!st.isFile()){res.writeHead(404);return res.end("Not found")}res.writeHead(200,{"Content-Type":MIME[path.extname(file).toLowerCase()]||"application/octet-stream","Cache-Control":"no-cache"});fs.createReadStream(file).pipe(res)})});
attach(server);server.listen(PORT,"0.0.0.0",()=>console.log(`LUMI unified server listening on ${PORT}`));
