import http from 'node:http';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {PORT} from './config.js';import {attach} from './websocket/websocketServer.js';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');
const MIME={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.webp':'image/webp','.svg':'image/svg+xml','.ico':'image/x-icon'};
const server=http.createServer((req,res)=>{res.setHeader('Access-Control-Allow-Origin','*');if(req.url==='/health'){res.writeHead(200,{'Content-Type':'application/json'});res.end(JSON.stringify({status:'ok',game:'LUMI'}));return}let u;try{u=new URL(req.url,'http://localhost')}catch{res.writeHead(400);return res.end('Bad request')}let p=path.resolve(ROOT,'.'+u.pathname);if(!p.startsWith(ROOT)||u.pathname.startsWith('/server/')){res.writeHead(403);return res.end('Forbidden')}if(u.pathname==='/'||!path.extname(p))p=path.join(ROOT,'index.html');fs.stat(p,(err,st)=>{if(err||!st.isFile()){res.writeHead(404);return res.end('Not found')}res.writeHead(200,{'Content-Type':MIME[path.extname(p).toLowerCase()]||'application/octet-stream','Cache-Control':u.pathname.startsWith('/assets/')?'public,max-age=86400':'no-cache'});fs.createReadStream(p).pipe(res)})});
attach(server);server.listen(PORT,()=>console.log(`LUMI unified server listening on ${PORT}`));
